export const metadata = {
  title: 'Easy Bearer Auth - Next.js',
  description: 'API moderna para autenticação Bearer Token com Sentus usando Next.js',
}

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
} 